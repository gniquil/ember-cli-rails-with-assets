import {
  railsAsset
} from '../../../helpers/rails-asset';
import { module, test } from 'qunit';

module('RailsAssetHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = railsAsset(42);
  assert.ok(result);
});
